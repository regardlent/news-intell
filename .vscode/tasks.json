{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Lancer l'analyse de news",
      "type": "shell",
      "command": "python -m src.cli executer --format md",
      "cwd": "${workspaceFolder}",
      "problemMatcher": [],
      "group": { "kind": "build", "isDefault": true }
    },
    {
      "label": "Exécuter les tests unitaires",
      "type": "shell",
      "command": "python -m unittest discover -s tests",
      "cwd": "${workspaceFolder}",
      "problemMatcher": []
    }
  ]
}
