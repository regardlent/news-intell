# Copiez ce fichier en `.env` et adaptez les valeurs si nécessaire.
# Le fichier `.env` ne doit jamais être publié dans le dépôt (voir .gitignore).

# URL de base de l'API LocalAI (conteneur Docker « local-ai »).
LOCALAI_BASE_URL=http://localhost:8080

# Clé d'API (facultatif).
LOCALAI_API_KEY=
